import { EnvironmentInterface } from "./core/environment.interface";

export const environment: EnvironmentInterface = {
    production: true,
    apiUrl: "http://localhost:9000/api/v1",
    version: "1.0",
}